#include "main"
/**
 * main - check the code
 *
 * Return: Always 0 (success)
 */
int _putchar(char c);
{

	char ch;
	for (ch = 'a'; ch <= 'z'; ch++)
		{
			putchar(ch);
		}
putchar('\n');
return (0);
}
